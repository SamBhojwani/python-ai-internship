The application uses a relational database to store structured data such as users, records and transactions. Database schemas are managed using SQLAlchemy models, with migrations tracked to ensure consistent structure across environments. Indexes should be added to columns that are frequently used in WHERE clauses or joins to improve query performance. Connection pooling is used to efficiently manage database connections under concurrent load. Regular backups are taken daily, with point in time recovery available for the last 7 days.
