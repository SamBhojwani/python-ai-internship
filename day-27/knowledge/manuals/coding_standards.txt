All code must pass linting and formatting checks before being merged into the main branch. Variable and function names should be descriptive and follow consistent naming conventions across the codebase. Every new feature should be accompanied by unit tests covering the main logic paths. Code reviews are mandatory for all pull requests, with at least one approval required before merging. Hardcoded secrets, API keys or credentials must never be committed to the repository.
